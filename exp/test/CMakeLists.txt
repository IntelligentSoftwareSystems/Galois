#makeTest(pairingheap)
#makeTest(skiplist)
#makeTest(dist)
makeTest(distforeach DISTSAFE COMMAND_PREFIX ${MPIEXEC} ${MPIEXEC_NUMPROC_FLAG} 2)
makeTest(thirdgraph DISTSAFE COMMAND_PREFIX ${MPIEXEC} ${MPIEXEC_NUMPROC_FLAG} 2)
#makeTest(foreachtask)
makeTest(networktest DISTSAFE COMMAND_PREFIX ${MPIEXEC} ${MPIEXEC_NUMPROC_FLAG} 2)
#makeTest(reduce)
makeTest(bag DISTSAFE)
makeTest(pairingheap DISTSAFE)
makeTest(skiplist DISTSAFE)
makeTest(networkbe DISTSAFE)
makeTest(networkfe DISTSAFE)
makeTest(netbandwidth DISTSAFE COMMAND_PREFIX ${MPIEXEC} ${MPIEXEC_NUMPROC_FLAG} 2)
makeTest(oneach DISTSAFE COMMAND_PREFIX ${MPIEXEC} ${MPIEXEC_NUMPROC_FLAG} 2)
makeTest(serialize DISTSAFE)
makeTest(fatpointerc DISTSAFE)
#makeTest(gasnet)
#makeTest(dirtest)
makeTest(CacheManagerTest DISTSAFE)
makeTest(LCDist DISTSAFE)
makeTest(ping DISTSAFE)
