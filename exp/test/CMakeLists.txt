function(makeTest name)
  add_executable(test-${name} ${name}.cpp)
  target_link_libraries(test-${name} galois)
  add_test(${name} test-${name})
endfunction()

makeTest(addhost)
makeTest(printhost)
#makeTest(pairingheap)
#makeTest(skiplist)
#makeTest(dist)
makeTest(distnodes)
makeTest(dirgraph)
makeTest(distgraph)
makeTest(dgraph)
#makeTest(foreachtask)
makeTest(networktest)
#makeTest(reduce)
makeTest(bag)
makeTest(pairingheap)
makeTest(skiplist)
makeTest(networkbe)
makeTest(networkfe)
makeTest(netbandwidth)
makeTest(fatpointerc)
#makeTest(gasnet)
#makeTest(dirtest)
makeTest(CacheManagerTest)
makeTest(LCDist)
