add_test_unit(ADD_TARGET bag)
add_test_unit(ADD_TARGET CacheManagerTest)
add_test_unit(ADD_TARGET distforeach COMMAND_PREFIX ${MPIEXEC} ${MPIEXEC_NUMPROC_FLAG} 2)
add_test_unit(ADD_TARGET fatpointerc)
add_test_unit(ADD_TARGET LCDist)
add_test_unit(ADD_TARGET mpi)
add_test_unit(ADD_TARGET netband2)
add_test_unit(ADD_TARGET netbandmpi)
add_test_unit(ADD_TARGET netbandwidth COMMAND_PREFIX ${MPIEXEC} ${MPIEXEC_NUMPROC_FLAG} 2)
add_test_unit(ADD_TARGET netbuffered)
add_test_unit(ADD_TARGET netraw)
add_test_unit(ADD_TARGET networkfe)
add_test_unit(ADD_TARGET networktest COMMAND_PREFIX ${MPIEXEC} ${MPIEXEC_NUMPROC_FLAG} 2)
add_test_unit(ADD_TARGET pairingheap)
add_test_unit(ADD_TARGET serialize)
add_test_unit(ADD_TARGET serialize-speed)
add_test_unit(ADD_TARGET serialize-vectors)
add_test_unit(ADD_TARGET skiplist)
add_test_unit(ADD_TARGET thirdgraph OMMAND_PREFIX ${MPIEXEC} ${MPIEXEC_NUMPROC_FLAG} 2)
add_test_unit(ADD_TARGET vertexCut)
