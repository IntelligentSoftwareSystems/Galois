if(USE_EXP)
  file(GLOB Sources *.cpp llvm/*.cpp mm/*.cpp ll/*.cpp ../exp/src/*.cpp)
else()
  file(GLOB Sources *.cpp llvm/*.cpp mm/*.cpp ll/*.cpp)
endif()

add_library(galois ${Sources})
if(USE_EXP)
  if(EXP_DOALL MATCHES "DMP")
    target_link_libraries(galois dmpruntime)
  endif()
endif()
